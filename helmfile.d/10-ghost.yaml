environments:
  production:
  ci:

repositories:
- name: bitnami
  url: https://charts.bitnami.com/bitnami

releases:
- name: ghost-staging
  chart: bitnami/ghost
  version: 12.3.9
  values:
  - ./config/ghost-values.yaml.gotmpl
