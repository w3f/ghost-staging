environments:
  production:
  ci:

repositories:
- name: bitnami
  url: https://charts.bitnami.com/bitnami

releases:
- name: ghost-staging
  chart: bitnami/ghost
  namespace: ghost
  version: 15.1.8
  values:
  - ./config/ghost-values.yaml.gotmpl
